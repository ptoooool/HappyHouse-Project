-- drop table housemember;

create table housemember(
   userid varchar(30) primary key,
    userpw varchar(30),
	username varchar(30),
    useraddress varchar(30),
    userphone varchar(30),
    userdong varchar(100)
);

commit;